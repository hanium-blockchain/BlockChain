extends layout

block content
    .mainPrice
        .row.price
            .col.price
                h3.price 블록체인 시세
                img.price(src='/images/blockchainGraph.png' alt='graph')
    
    .menu
        .list
            h3 투자 내역 리스트
        .asset
            h3 보유 자산 